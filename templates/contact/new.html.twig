{% extends 'base.html.twig' %}

{% block title %}New Contact{% endblock %}

{% block body %}
<div class="row">
    <div class="col-md-6 mx-auto">
    <h1>Create new Contact</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

        <div class="d-grid gap-2">
            <input type="submit" value="{{ submitButtonText ?? 'Send' }}"class="btn btn-primary " ></div>
    {{ form_end(form) }}
    </div></div>


{% endblock %}
